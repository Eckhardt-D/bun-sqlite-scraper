{
  "name": "bun-sqlite-scraper",
  "description": "Because why not",
  "module": "index.ts",
  "type": "module",
  "author": "Eckhardt-D",
  "license": "MIT",
  "scripts": {
    "test": "bun run index.ts",
    "docker:build": "docker build -t bun-sqlite-scraper .",
    "docker:run": "docker run --rm -it -v \"$PWD\":/app bun-sqlite-scraper /bin/bash"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "sql-template-tag": "^5.2.1",
    "sqlite-html": "^0.1.3",
    "sqlite-http": "^0.1.1"
  }
}
